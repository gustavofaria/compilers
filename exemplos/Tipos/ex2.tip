programa
   a, b, soma: inteiro;

   funcao somador(x: inteiro, y: inteiro) : inteiro
   r: inteiro;
   inicio
      r := x + y;
      retorne r;
   fim ;

inicio
   a := 2;
   b := 3;
   soma := somador(a+1, b+2);
   saida a, b, soma;
fim;
